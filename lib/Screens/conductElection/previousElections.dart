import 'package:flutter/material.dart';

class PreviousElectionsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Previous Elections'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Previous Elections Screen'),
      ),
    );
  }
}
